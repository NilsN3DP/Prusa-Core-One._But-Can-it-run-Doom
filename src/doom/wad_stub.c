/* Temporary WAD stub so the engine links while we measure code size.
 * Replaced later by the reduced WAD (C array) baked into internal flash. */

const unsigned char doom_iwad[1] = { 0 };
const unsigned int doom_iwad_len = 0;
